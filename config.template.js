// Fichier généré automatiquement par le workflow GitHub Actions à partir
// des Secrets du repo (Settings > Secrets and variables > Actions).
// Ne pas éditer à la main : ce fichier n'est PAS versionné (voir .gitignore),
// il n'existe que dans le build déployé sur GitHub Pages.
window.EVE_SSO_CONFIG = {
  CLIENT_ID: "__EVE_CLIENT_ID__",
  CALLBACK_URL: "__EVE_CALLBACK_URL__",
  SCOPES: "esi-wallet.read_character_wallet.v1"
};
